export interface Anime{
    id: Number,
    nome: string,
    imagemURL: string,
    temporadaAnime: number,
    temporadaLancamento: string[],
    estudio: string[],
    generos: string[],
    fonte: string,
    pontos: number
}